<Page x:Class="SharpChat.Wpf.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SharpChat.Wpf.Pages"
      xmlns:vm="clr-namespace:SharpChat.Core.ViewModels;assembly=SharpChat.Core"
      xmlns:converters="clr-namespace:SharpChat.Wpf.Converters"
      mc:Ignorable="d" 
      d:DesignHeight="200" d:DesignWidth="300"
      d:Background="White"
      d:DataContext="{d:DesignInstance Type=vm:LoginViewModel, IsDesignTimeCreatable=False}">

    <Page.Resources>
        <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </Page.Resources>
    
    <Grid Width="250" Margin="24, 16">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="5*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="2"
                   Text="Sign In"
                   HorizontalAlignment="Center"
                   FontSize="24" 
                   Margin="0, 8"/>

        <TextBlock Grid.Row="1"
                   Text="User Name: " 
                   VerticalAlignment="Center" />

        <TextBox Grid.Row="1"
                 Grid.Column="1"
                 Text="{Binding Username, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 d:Text="TestUser"
                 Margin="0, 4"/>

        <TextBlock Grid.Row="2"
                   Text="Password: " 
                   VerticalAlignment="Center" />

        <PasswordBox Grid.Row="2"
                     Grid.Column="1" 
                     Margin="0, 4"
                     d:Password="TestPassword"
                     PasswordChanged="PasswordBox_PasswordChanged"/>

        <Button Grid.Row="3"
                Grid.ColumnSpan="2"
                Content="Sign In"
                Margin="0, 8"
                Command="{Binding LoginCommand}"/>

        <TextBlock Grid.Row="4"
                   Grid.ColumnSpan="5"
                   VerticalAlignment="Top"
                   HorizontalAlignment="Center"
                   Foreground="Red"
                   Visibility="{Binding IsErrorMessageVisible, Converter={StaticResource BooleanToVisibilityConverter}}"
                   Text="Login/password are not correct, try again..." />

    </Grid>
</Page>
